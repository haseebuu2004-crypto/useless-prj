# Human Tribunal - Frontend

React + Vite frontend for the Human Tribunal absurd courtroom experience.

## Running Locally

```bash
npm install
npm run dev
```

App will start at `http://localhost:5173`.

## Environment Configuration

Create a `.env` file in this directory:
```env
VITE_API_URL=http://localhost:3000
```
